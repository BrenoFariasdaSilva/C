/*

7.5 Execute as seguintes tarefas:

a) Escreva um protótipo de função para uma função
chamada exchange, que usa dois ponteiros para
números em ponto flutuante x e y como parâmetros
e não retorna um valor.

void exchange (float *x, float *y);

b) Escreva um protótipo de função para a função na
parte (a).

void exchange (float *x, float *y);

c) Escreva o protótipo da função para uma função chamada
evaluate que retorna um inteiro e que usa como parâmetros 
o inteiro x e um ponteiro para a função poly. 
A função poly usa um parâmetro inteiro e retorna um inteiro.

int evaluate (int x, int (*poly)(int));

d) Escreva o protótipo de função para a função na
parte (c).

int evaluate (int x, int (*poly)(int));

*/

#include <stdio.h>

int main()
{
    
    return 0;
}
