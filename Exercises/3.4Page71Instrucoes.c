/*

3.4 Escreva uma instrução em C que possibilite realizar as
seguintes tarefas:
a) declarar variáveis soma e x para que sejam do tipo
int.
b) inicializar variável x em 1.
c) inicializar variável soma em 0.
d) somar variável x à variável soma e atribuir o resultado
à variável soma.
e) imprimir 'A soma é:' seguido pelo valor da variável
soma.

RESPOSTA:

a) int soma, x;

b) x = 1;

c) soma = 0;

d) soma += x;

e) printf("A soma é: %d", soma);
*/

