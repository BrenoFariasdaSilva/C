/*

6.12 Escreva instruções isoladas que realizem cada uma das
operações de array de subscrito único a seguir:
a) Inicializem os 10 elementos do array de inteiros
counts com zeros.

for (int i = 0; i < 10; i++)
{
    a[i] = 0;
}

b) Somem 1 a cada um dos 15 elementos do array de
inteiros bonus.

for (int i = 0; i < 15; i++)
{
    a[i] += 1;
}

c) Leiam os 12 valores do array de ponto flutuante
tempPorMes via teclado.

for (int i = 0; i < 12; i++)
{
    printf("Digite o valor do array[%d]: \n", i);
    scanf("%f", &tempPorMes[i]);
}

d) Imprimam os cinco valores do array de inteiros bestScores
em formato de coluna.

for (int i = 0; i < 5; i++)
{
    printf("%d\t", bestScores[i]);
}

*/

#include <stdio.h>

int main()
{
    
    return 0;
}
